# CLAUDE.md

## 项目概述

这是一个个人工具集项目，用于存放各种小项目、脚本和工具。

## 项目结构

```
./
├── configs/      # 配置文件目录
├── scripts/      # 脚本文件目录
├── readme.md     # 项目说明文档
├── .env          # 环境变量配置
└── .gitignore   # Git 忽略配置
```

## 技术栈

- 主要语言：Shell、Bash
- 配置文件：JSON、YAML 等

## 编码规范

- **注释和文档使用中文**
- 代码风格保持简洁一致
- 优先使用 shell 原生功能

## 常用命令

根据具体脚本而定，请查看 `scripts/` 目录下的脚本文件。

## 注意事项

- 所有环境敏感信息（如 API Key、密码等）存储在 `.env` 文件中
- `.env` 文件已加入 `.gitignore`，不会提交到版本库

## 待补充

- [ ] 具体的目录结构说明
- [ ] 各脚本的用途说明
- [ ] 特定的依赖或环境要求
